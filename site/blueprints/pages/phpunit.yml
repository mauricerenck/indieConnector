options:
  changeSlug: false
  update: true

sections:
  phpunit:
    type: fields
    fields:
      indieConnetor:
        extends: indieconnector/fields/webmentions
      textfield:
        type: textarea
        label: Text
      layouteditor:
        type: layout
        label: Layout
      blockeditor:
        type: blocks
        label: Blocks
