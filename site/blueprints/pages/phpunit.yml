options:
  changeSlug: false
  update: false

sections:
  phpunit:
    type: fields
    fields:
      textfield:
        type: textarea
        label: Text
      layouteditor:
        type: layout
        label: Layout
      blockeditor:
        type: blocks
        label: Blocks
